<h1>Startup creation</h1>

<form action="{{ path('admin_startup_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_startup') }}">
            Back to the list
        </a>
    </li>
</ul>
